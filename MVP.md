# MVP 任务与验收

依据用户提供的《MVP 实施方案 v1.0》全部 35 节。这里是工程任务拆分，非最终完成声明。

| Phase | Issue | 验收重点 |
| --- | --- | --- |
| 0 | #1 | 目录、文档、虚拟环境、可运行 FastAPI + SQLite、秘密忽略 |
| 1 | #2 | 管理员登录、数据库持久化、移动控制台、Caddy HTTPS |
| 2 | #3 | Gateway 幂等部署、状态与错误、SSH/sing-box/资源检测 |
| 3 | #4 | SOCKS5 测试、真实出口、固定 IP 身份守卫 |
| 4 | #5 | Exit Group、配置校验、备份、应用、出口匹配、回滚 |
| 5 | #6 | 设备登记与持久化 |
| 6 | #7 | 两台设备绑定一个订阅组，改 ISP 必须确认 |
| 7 | #8 | 单节点 Shadowrocket 订阅、令牌轮换、两手机导入 |
| 8 | #9 | 1/5 分钟监控、资源指标、故障识别、质量评分 |
| 9 | #10 | 八个页面、7/3/1 天提醒、绑定历史、审计告警 |
| 10 | #11 | 完整真实链路与故障注入验收 |

## 阶段门槛

每阶段先实现、再验证、独立提交。未实现模块保留目录与 Issue，不用假数据填充可用功能。
阶段 1 的真实 HTTPS 验收需要 Controller VPS、域名和 DNS；阶段 2 需要 Gateway VPS 及 SSH 信任材料。
阶段 3 起需要 SOCKS5 ISP；最终验收需要两台 iPhone + Shadowrocket。

## 最终现场测试（当前全部未执行）

1. 两台 iPhone 导入同一个订阅。
2. 两台手机实际检测出口为同一个 expected_exit_ip。
3. 重启 sing-box，观察故障与恢复。
4. 故意错误的 ISP 密码不能得到 Healthy。
5. 出口 IP 变化必须 CRITICAL，不能自动接受新身份。
6. 重启 Gateway，Controller 识别 DOWN → HEALTHY。
7. 到期设为 7 天，出现提醒。
8. 改 Group ISP 必须人工确认旧/新 IP。
9. History 记录何时、操作者、旧/新 ISP。
10. 手机浏览器操作核心功能可用。

通过软件单元/集成测试不能代替以上现场证据。最终完成定义是两台手机稳定共享静态 ISP 出口，且控制台显示真实状态。

