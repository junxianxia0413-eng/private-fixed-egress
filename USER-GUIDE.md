# Private Fixed-Egress Network Manager 使用说明

## 这个控制台能做什么

这个系统把两台手机的网络流量送到同一个固定公网出口。它负责管理服务器、静态 ISP 代理、出口线路、手机订阅、自动检测、故障保护、告警和操作历史。

实际控制台地址和固定出口以部署时生成的私密接入说明为准。没有域名也可以使用，公网 IPv4 的 HTTPS 证书由服务器自动续期。

正常链路如下：

```text
iPhone / Shadowrocket
        ↓ 加密连接
东京 Gateway 服务器
        ↓ 仅允许已绑定的 SOCKS5 ISP
已验证的固定 ISP 出口
        ↓
互联网
```

如果固定出口无法通过身份检测，手机入口会自动关闭；系统不会偷偷改用 VPS 自身公网 IP。

## 最常用的操作

### 给两台 iPhone 导入订阅

1. 登录控制台，打开“订阅组”。
2. 找到 `PHONE-GROUP-01`，点击显示订阅。
3. 选择一种导入方式：
   - 在另一台设备显示二维码，用 iPhone 相机或 Shadowrocket 扫码；
   - 复制订阅链接，在 Shadowrocket 中添加 `Subscribe` 类型。
4. 更新订阅，选择唯一的 `PHONE-GROUP-01` 节点。
5. 打开 Shadowrocket，首次使用建议将“全局路由”设为“代理”。
6. 两台手机分别查询公网 IP，都应显示私密接入说明中记录的固定出口。

二维码和链接包含私密订阅令牌。不要发到公开聊天、GitHub Issue 或客户工单。两台手机共用一个订阅，因此系统不能仅凭链接判断是哪一台物理手机。

### 日常检查

登录后先看“概览”。如果当前告警为 0、Gateway 和 ISP 都正常，通常不需要操作。需要看线路细节时进入“网络监控”，需要追查异常时进入“告警”和“历史记录”。

### 订阅链接泄露时

在订阅组中轮换订阅令牌。旧订阅地址会立即失效，新地址需要重新保存到两台手机。已经导入手机的节点密码不会因为仅轮换地址而自动改变。

## 每个页面的作用

### 概览 Overview

显示已登记的设备、订阅组、ISP 和网关数量，以及当前告警数。它适合每天快速确认系统是否正常。

### 设备 Devices

保存手机的管理记录，包括名称、类型、系统、用途和备注。设备登记用于管理，不代表手机已经成功连接。

当前有 `Phone-01` 和 `Phone-02`。一个设备只能属于一个订阅组，避免重复分配。

### 服务器 Gateways

管理运行控制台和网络核心的 Debian 服务器。可以部署、检测和重启受管服务。服务器使用固定 SSH 主机身份和密钥登录，页面不会显示私钥或原始密码。

常见状态：

- `HEALTHY`：服务、配置、防火墙、入口和密钥保护均通过。
- `CHECKING`：正在执行检查。
- `CRITICAL`：检查失败，需要查看告警或任务错误。
- `UNKNOWN`：检测结果过期，需要重新检查。

### 固定出口 ISP Exits

保存静态 SOCKS5 ISP 的连接信息和预期固定公网 IP。系统通过两个独立 HTTPS 服务核对出口身份。

页面的“完整请求耗时”包含 SOCKS5 连接、TLS 握手和检测网站响应，不是单纯的网络延迟。真正用于判断 Gateway 到 ISP 入口质量的数值在“网络监控”页面。

固定出口第一次验证后会锁定。检测到不同 IP 时会进入严重状态，不会自动把新 IP 当作正确出口。

### 出口线路 Exit Groups

把一个 Gateway 和一个 ISP 组成可用线路。应用配置时会先备份旧配置，再检查新配置、真实出口和手机加密入口。失败时恢复旧配置；控制台失联时远程回滚计时器也会恢复旧配置。

### 订阅组 Groups

把 1–20 台设备绑定到一个出口线路，并生成一个共享订阅。设备名称可以直接输入或修改，右侧菜单可以移除设备，“添加设备”可以继续增加成员。输入已有名称会绑定设备库记录，输入新名称会自动创建设备。更换出口时会显示旧 ISP、新 ISP、旧 IP 和新 IP，必须再次确认；系统会在历史记录中保存这次操作。

### 网络监控 Network Health

约每分钟检查服务和资源，每五分钟测量一次线路质量：

- TCP 连接延迟：Gateway 到 ISP 代理入口建立连接的时间。
- TCP 抖动：多次连接延迟的变化程度。
- TCP 连接失败：十次尝试中失败的次数。
- ICMP 丢包率：仅在 ISP 入口回复 ICMP 时显示；不回复时显示“未知”。
- 近一小时检查通过率：已执行健康检查的成功比例，不代表长期 SLA。
- 接收/发送速率：服务器主网卡的短时变化，不等于单台手机速度。
- 入口 TCP 连接：当前连接数量，不等于设备数量。

手机实际体验还包含“手机所在城市和运营商 → 东京 Gateway”这一段，因此必须用真实手机网络测试。

### 告警 Alerts

自动记录 Gateway、ISP、出口线路异常和 ISP 到期提醒。相同异常持续出现时只保留一条并更新时间，恢复后记录解决时间。

当前版本在控制台内显示告警，没有自动发送短信、邮件或聊天通知。

### 历史记录 History

记录操作者、时间、操作和变更详情，可分页查看。创建、检测、部署、绑定变更、订阅轮换和故障恢复都会留下记录。凭据和私密订阅令牌不会写进历史。

## 为什么检测看起来比较慢

“服务器检测”需要建立加密管理连接，然后检查服务、配置、防火墙、密钥登录、CPU、内存和磁盘。“出口检测”还必须通过真实 ISP 代理访问两个 HTTPS 网站并确认结果一致。它们是完整检查，所以比普通 `ping` 慢。

系统已将两个出口检测站改为并行执行，并将后台任务响应间隔从最多 2 秒缩短到 0.5 秒。即使页面显示约 200 ms，也不能直接说明 VPS 慢；当前 Gateway 到 ISP 入口的 TCP 延迟约为 1–2 ms，差异主要来自检测网站响应和手机到东京的线路。

## 出现问题时怎么处理

1. 先看“概览”的当前告警数量。
2. 打开“告警”，确认是 Gateway、ISP 还是出口线路。
3. 打开“网络监控”，确认 TCP 连接是否失败以及检测是否过期。
4. 在对应页面执行一次“立即检测”。
5. 如果 Gateway 服务异常，可在“服务器”页面执行重启。
6. 如果固定出口 IP 发生变化，不要修改预期 IP 掩盖问题；先联系 ISP 服务商确认。

入口因检测失败关闭后，系统会继续复查；相同固定身份恢复时，入口会自动重新开放。

入口保护按出口线路独立运行。一条 ISP 短暂抖动不会再关闭其他正常订阅；单次临时失败会自动重试并进入一个短暂容错周期，连续失败才关闭该线路。固定出口 IP 一旦变成其他地址仍会立即关闭，系统不会为了保持连接而使用错误出口。

## 备份和安全

控制台数据库、秘密目录和服务器环境文件需要一起备份。升级前服务器会自动创建数据库和秘密快照，失败时恢复旧版本。仓库不会保存 ISP 密码、SSH 私钥、订阅令牌、生产数据库或备份。

会产生费用、更换真实 ISP、删除 Gateway/ISP 或重装服务器的操作不应自动执行。

## 当前限制

- 手机入口已验证 TCP；UDP 游戏、语音和部分实时应用仍需逐项测试。
- 两台真实 iPhone 的 Wi-Fi/蜂窝、IPv6 和 DNS 行为需要手机到手后完成。
- 当前是单台服务器同时运行 Controller 和 Gateway；服务器重启期间控制台也会暂时不可访问。
