# Run python -m scripts.setup to create a local .env and administrator.
# No default password. Do not commit .env or database/secrets/backups.
APP_ENV=development
PUBLIC_URL=http://127.0.0.1:8000
DATABASE_PATH=database/controller.db
SESSION_HOURS=8

