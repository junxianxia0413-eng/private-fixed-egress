# 自动化操作边界

依据项目需求：可以检查、部署、测速、应用配置、重启服务和回滚；实际操作需要有用户提供或授权的目标资源。

以下动作需要明确确认：更换 TK ISP、删除 ISP/Gateway、重装服务器、购买资源或产生费用。
不要改写 expected_exit_ip 来掩盖漂移。不要删除旧配置而失去回滚能力。

每个模块完成后运行相关验证，再提交代码。按 MVP.md 阶段顺序开发，不能同时展开全部模块。
真实资源测试未执行时，在验收记录中写明“未执行”及缺少的资源，不能写 PASS。
不提交 .env、密码、私钥、订阅令牌、数据库、备份及含秘密的日志。

